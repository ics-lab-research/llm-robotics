```URscipt
def rpy2rotmat(rpy):
	
	gamma = rpy[0]	#roll
	beta = rpy[1]	#pitch
	alpha = rpy[2]	#yaw

	# trigonometric
	ca = cos(alpha)
	cb = cos(beta)
	cg = cos(gamma)
	sa = sin(alpha)
	sb = sin(beta)
	sg = sin(gamma)
  
	# column 1
	r11 = ca*cb
	r21 = sa*cb
	r31 = -sb
	# column 2
	r12 = ca*sb*sg-sa*cg
	r22 = sa*sb*sg+ca*cg
	r32 = cb*sg
	# column 3
	r13 = ca*sb*cg+sa*sg
	r23 = sa*sb*cg-ca*sg
	r33 = cb*cg

	# elements are represented as an array
	rotmat = [ r11, r21, r31, r12, r22, r32, r13, r23, r33 ]
	
	return rotmat
end
```
